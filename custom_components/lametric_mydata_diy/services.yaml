refresh:
  name: Refresh feed
  description: Render the current LaMetric JSON payload immediately.
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry ID. If omitted, all configured feeds are refreshed.
      required: false
      selector:
        text:
